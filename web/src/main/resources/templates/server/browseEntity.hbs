{{#partial "title"}}Scout - Browse - Entity{{/partial}} {{#partial
"NavBrowseClass"}}active{{/partial}} {{#partial "content"}}
<div class="container">
	{{#entity}}
	<div class="page-header">
		<ul class="breadcrumb">
			<li><a href="{{mustacheletPath}}/browse">Categories</a></li>
			<li><a href="{{mustacheletPath}}/browse/type/{{type.id}}">{{type.name}}</a></li>
			<li class="active">{{name}}</li>
		</ul>
		<h1>Entity</h1>
	</div>

	<dl class="dl-horizontal">
		<dt>Name</dt>
		<dd>{{name}}</dd>
	</dl>

	<h4>Properties</h4>
	<table class="table table-striped">
		<thead>
			<tr>
				<th>Name</th>
				<th style="width: 100%">Value</th>
				<th style="width: 10%">Action</th>
			</tr>
		</thead>
		<tbody>
			{{#values}}
			<tr>
				<td style="white-space: nowrap;">{{property.name}}</td>
				<td>{{#value-render this short=true}}{{/value-render}}</td>
				<td><a class="btn btn-small btn-link"
					href="{{../mustacheletPath}}/browse/value/{{id}}"><i
						class="glyphicon glyphicon-th-list"></i></a></td>
			</tr>
			{{/values}}
		</tbody>
	</table>
	{{^values}}
	<div class="alert alert-warning">
		<b>Empty!</b> There are no defined property values for this entity.
	</div>
	{{/values}} {{/entity}} {{^entity}}
	<div class="alert alert-error">
		<b>Not found!</b> Could not find entity {{id}}.
	</div>
	{{/entity}}
</div>
{{/partial}} {{>base}}
