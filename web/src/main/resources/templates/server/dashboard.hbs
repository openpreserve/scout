{{#partial "title"}}Scout - Dashboard{{/partial}} {{#partial
"NavDashboardClass"}}active{{/partial}} {{#partial "content"}}
<div class="container">
	<div class="page-header">
		<h1>Dashboard</h1>
	</div>
	<p>All about your own information.</p>

	<h2>My triggers</h2>
	{{#if requests}}
	<table class="table table-striped">
		<thead>
			<tr>
				<th>Name</th>
				<th style="width: 10%">Action</th>
			</tr>
		</thead>
		<tbody>{{#each requests}}
		<tr>
			<td>{{description}}</td>
			<td><a class="btn btn-small btn-link"
				href="{{mustacheletPath}}/browse/request/{{id}}"><i class=" icon-eye-open"></i></a></td>
		</tr>{{/each}}
		</tbody>
	</table>
	{{/if}}
	{{#unless requests}}
	<div class="alert alert-warning">
		You have no triggers defined, create one now!
	</div>
	{{/unless}}
	<a class="btn btn-success" href="{{mustacheletPath}}/request/new"><i class="icon-plus icon-white"></i> Create trigger</a>
</div>
<div class="container">
   <h2>
    My policies
   </h2>
   {{#if objectives}}
    <table class="table table-striped">
        <thead>
            <tr>
                <th style="width: 10%">Objective</th>
                <th style="width: 10%">Measure</th>
                <th style="width: 30%">Description</th>
                <th style="width: 5%">Modality</th>
                <th style="width: 5%">Qualifier</th>
                <th style="width: 10%">Value</th>
            </tr>
        </thead>
        <tbody>
            {{#each objectives}}
            <tr>
                <td style="white-space: nowrap;">{{url}}</td>
                <td>{{measure}}</td>
                <td>{{measureDescription}}</td>
                <td>{{modality}}</td>
                <td>{{qualifier}}</td>
                <td>{{value}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    {{/if}}
    {{#unless objectives}}
    <div class="alert alert-warning">
        <b>Empty!</b> There are no defined policies.
    </div>
    {{/unless}}
     <a class="btn btn-success" href="{{mustacheletPath}}/upload/policy/new"><i class="icon-plus icon-white"></i>Upload policies</a>
</div>
{{/partial}} {{> base}}
